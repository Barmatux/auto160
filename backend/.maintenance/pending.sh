#!/usr/bin/env bash
# No pending maintenance tasks.
# Add commands here for a one-shot post-deploy job; deploy-vm.sh removes this
# file from the VM after a successful run (re-add via git when needed).
set -euo pipefail
echo "==> No pending maintenance"
exit 0
